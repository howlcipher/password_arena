# Contributing

1. Create a branch from `main`.
2. Keep all examples synthetic and local.
3. Add or update tests for behavior changes.
4. Run `ruff check .`, `mypy src/password_arena`, and `pytest`.
5. Explain how the change preserves the safety boundaries in `SECURITY.md`.
